{
  "name": "immo-find-api",
  "version": "1.0.0",
  "description": "Immobilien Scraping API",
  "main": "main.py",
  "scripts": {
    "start": "uvicorn main:app --host=0.0.0.0 --port=$PORT"
  },
  "dependencies": {
    "cors": "^2.8.5"
  },
  "engines": {
    "python": "3.9.x"
  }
}
